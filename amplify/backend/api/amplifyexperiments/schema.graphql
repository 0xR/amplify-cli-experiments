type Category @model {
  id: ID!
  name: String!
  products: [Product] @connection(name: "CategoryProductsConnection")
}
type Product @model {
  id: ID!
  title: String!
  description: String!
  category: Category @connection(name: "CategoryProductsConnection")
}
